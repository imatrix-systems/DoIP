# Usability / CLI Review — doip_test.py

**File:** `/home/greg/iMatrix/DOIP/DoIP_Server/test/scripts/doip_test.py`
**Reviewer:** Usability/CLI Agent
**Date:** 2026-03-09

---

## Checklist

### 1. Shebang line present (`#!/usr/bin/env python3`)
**PASS** — Line 1: `#!/usr/bin/env python3`. Correct portable form.

### 2. `__name__ == "__main__"` guard
**PASS** — Lines 514-515: standard guard calls `main()`. Script is safe to import as a library module without side effects.

### 3. argparse with description, per-flag help strings
**PASS** — `argparse.ArgumentParser` with a clear description (lines 454-456). Every argument (`--host`, `--port`, `--vin`, `--eid`, `--verbose`) has a `help=` string that includes the default value. Help strings are concise and informative.

### 4. Short flags: `-H` for host, `-p` for port, `-v` for verbose
**PASS** — All three short flags present:
- `-H` / `--host` (line 457)
- `-p` / `--port` (line 459)
- `-v` / `--verbose` (line 465)

Good choice of `-H` (uppercase) to avoid collision with `-h` (help).

### 5. Defaults match server config
**PASS** — Defaults align with `doip-server.conf`:
- `DEFAULT_HOST = "127.0.0.1"` matches `bind_address = 127.0.0.1`
- `DEFAULT_PORT = 13400` matches `tcp_port = 13400` / `udp_port = 13400`
- `DEFAULT_VIN = "APTERADOIPSRV0001"` matches `vin = APTERADOIPSRV0001`
- `DEFAULT_EID = "00:1A:2B:3C:4D:5E"` matches `eid = 00:1A:2B:3C:4D:5E`

Zero-configuration run against a default server works out of the box.

### 6. `--verbose` / `-v` prints raw TX/RX hex
**PASS** — Global `VERBOSE` flag set from `args.verbose` (line 482). `log_tx()` and `log_rx()` functions (lines 127-133) print full hex dumps prefixed with `TX`/`RX` labels and transport tags (`[UDP]`, `[TCP]`). Both UDP and TCP paths call these helpers.

### 7. Output format clear and useful for debugging
**PASS** — Output is well-structured:
- Banner with server address at startup.
- Pre-flight reachability check with actionable error ("is the server running?").
- Section headers (`--- UDP Discovery ---`, `--- TCP Queries ---`).
- Each test prints `[PASS]`/`[FAIL]` with relevant details (VIN, addresses, codes).
- Summary line with pass/fail counts.
- Port validation and EID format validation with errors to stderr.
- Exit code 0 on success, 1 on failure (CI-friendly).

### 8. Help text (`-h`) useful
**PASS** — The description clearly states the script's purpose (UDP discovery + TCP diagnostics). Each flag's help string shows the default value. The standard `-h`/`--help` is auto-generated by argparse.

### 9. Script is `chmod +x` executable
**PASS** — File permissions are `755` (owner rwx, group rx, other rx).

### 10. Python 3.6+ compatible (no walrus operator, no 3.10+ features)
**PASS** — No walrus operator (`:=`), no `match`/`case` statements, no type union syntax (`X | Y`). Uses only f-strings (3.6+), `bytes.hex()` (3.5+), standard library modules. Compatible with Python 3.6+.

---

## Overall Verdict: **PASS** (10/10)

The script is production-ready from a CLI usability standpoint. All flags are well-documented, defaults match the server configuration, verbose mode provides actionable hex dumps, output is structured for both human reading and CI consumption, and the script runs correctly on Python 3.6+.
